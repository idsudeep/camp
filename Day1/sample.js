/**
 * Write a program which should take two inputs and 
 * sum the inputs and give the output.
 */

a = 10;
b = 20;

/**
 * ES6 Code style
 */
sumOfTwo = (first, second) => {
    return first + second;
}

c = sumOfTwo(a, b);
console.log(c);